import { Container } from '../../core'

import './header.scss'
export function HeaderComponent() {
    return (
        <>
            <Container fullWidth={true} spacingHeight={24} className="Container-header" />
        </>
    )
}
